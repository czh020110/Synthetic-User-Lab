{
  "name": "synthetic-user-lab",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"uv run uvicorn backend.main:app --host 127.0.0.1 --port 8765 --reload\" \"cd frontend && npm run dev\"",
    "dev:api": "uv run uvicorn backend.main:app --host 127.0.0.1 --port 8765 --reload",
    "dev:web": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
