# 常用命令

## 安装依赖

```bash
conda activate synthetic-user-lab
python -m playwright install
```

## 启动开发服务

```bash
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8765
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8765 --reload
```

## 运行测试

```bash
python -m pytest
```

## 类型检查

```bash
当前暂无
```

## Lint / 格式化

```bash
当前暂无
```

## 构建

```bash
当前暂无
```

## 图片生成

```bash
python .claude/skills/image-generate/scripts/generate_image.py --prompt "Optimized English image prompt here" --size 2048x2048
```

## 数据库 / 迁移

```bash
当前暂无
```
