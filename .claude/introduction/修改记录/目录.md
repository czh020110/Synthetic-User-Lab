# validator

## 使用 success_criteria 列表判断任务成功

# demo_runs

## pending task 占位记录改用 success_criteria

# config

## 规范化 api_prefix 确保 FastAPI 路由前缀合法

# run_schemas

## 调整 RunRequest 与 DemoTask 的职责边界

# graph_prompt

## 统一 system prompt 与动态 prompt 的占位符职责

# demo_run_graph

## 将 persona/task 固定上下文注入 run 级 agent system prompt

# graph_prompt

## 为 decide agent 输入补充历史步骤详情

# demo_run_graph

## 按 run_id 区分 agent checkpointer 并显式传入历史步骤

# graph_prompt

## 新增 validate agent 的验证提示词与输入模板

# demo_run_graph

## 将 validate_progress 节点接入 validate agent

# config

## 支持从项目根目录 .env 加载运行配置

# run_schemas

## 补充 DemoPersona 用户画像字段注释

# demo_run_graph

## 修复 run 异常失败时 error_message 为空、report 长期 409 且日志不清晰的问题

# in_memory_run_store

## 修复 complete_run 清空失败错误信息导致状态排查困难的问题

# playwright_adapter

## 修复浏览器会话创建失败时缺少关键日志的问题
