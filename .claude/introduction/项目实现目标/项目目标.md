# 项目目标

## 总体流程目标

以固定 demo persona、固定 demo task 和内置 demo 页面为起点，先跑通从 FastAPI 启动 run、LangGraph 编排执行、Playwright 观察与交互、agent 决策与验证、步骤日志沉淀到最终报告输出的最小纵向闭环；在此基础上逐步扩展为支持多 persona、多任务模板、RAG 上下文、失败恢复、摩擦分析和正式持久化的 Synthetic User Lab。

## 核心功能目标

- [ ] 提供可启动和查询的 demo run API：验收方式：可通过 `/runs/demo/start`、`/runs/demo/{id}`、`/runs/demo/{id}/steps`、`/runs/demo/{id}/report` 完成一次 run 生命周期验证。
- [ ] 提供受控动作执行与页面观察能力：验收方式：Playwright 能在 demo 页面完成 navigate、click、fill、wait 等动作，并生成 `ObservedPageState` 与 `ExecutionResult`。
- [ ] 提供 run 级 agent 决策与验证能力：验收方式：`decide_agent` 与 `validate_agent` 可基于 persona、task、页面事实和历史步骤输出结构化 `ActionInput` 与 `ValidationResult`。
- [ ] 提供最小状态沉淀与收尾能力：验收方式：内存存储可记录 `RunRecord`、`StepLog`、`RunReport`，异常失败时仍能返回失败报告。

## 阶段功能范围

- 最小 Demo Run 闭环：目标：打通固定 demo persona、固定 demo task、固定 demo 页面的一次 run 执行、日志与报告闭环；状态：完成；依赖：无。
- Agent 化决策与验证：目标：将 decide 与 validate 两个语义节点都接入 run 级 agent，并区分固定 prompt 与动态输入职责；状态：完成；依赖：最小 Demo Run 闭环。
- 失败信息与本地配置补强：目标：补齐 `.env` 配置加载、API 前缀规范化、失败原因保留和异常报告兜底；状态：完成；依赖：最小 Demo Run 闭环。
- 停止条件与任务验证增强：目标：根据成功条件、步数、重复动作和错误信号稳定判断继续、成功或失败；状态：进行中；依赖：Agent 化决策与验证。
- 持久化、RAG 与摩擦分析扩展：目标：接入正式存储、上下文检索、失败恢复和结构化摩擦分析；状态：未开始；依赖：停止条件与任务验证增强。

## 实现发现

- 当前仓库已不只是纯规则式 demo run，`demo_run_graph` 中决策与验证节点都已改为 agent 结构化输出，但状态存储仍为 `InMemoryRunStore`。
- 当前 task 成功判定已从单一 `success_text` 收敛为 `success_criteria` 列表，启动请求与任务定义的职责边界更清晰。
- run 失败链路已补齐最小失败报告与错误信息保留机制，说明当前阶段重点已从“能跑起来”转向“失败也能排查和回放”。
- 项目文档、TODO 与核心数据流存在旧格式和占位内容，需要按当前真实实现重新对齐。

## 后续目标调整

- 下一阶段优先完成验证与停止条件收敛，避免 agent 决策链路在失败、卡住和重复动作场景下缺少稳定退出规则。
- 持久化替换应优先兼容现有 `run_store` 接口，减少对 `demo_run_graph` 主流程的破坏。
- RAG、失败恢复和摩擦分析建议沿现有 observe / decide / validate / log 主链路增量接入，而不是重新设计一套并行流程。