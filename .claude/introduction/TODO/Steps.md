- [ ] 搭建项目基础目录结构：创建 backend、frontend、data、reports、screenshots、docker 等核心目录，并明确后端 api、core、graph、execution、retrieval、analysis、db、schemas 分层。
- [ ] 初始化后端 FastAPI 应用：建立应用入口、配置管理、健康检查接口、基础路由注册方式和统一响应结构。
- [ ] 定义核心数据结构：实现 Project、Persona、Task、Run、StepLog、FrictionIssue、KnowledgeArtifact、FailureCase 等 Pydantic / ORM 模型。
- [ ] 建立持久化与本地文件存储：接入 PostgreSQL 或阶段性 SQLite，保存 run、step、report、persona、task，并规划截图与原始日志存储路径。
- [ ] 实现 Persona 与 Task 管理能力：支持创建 3 类基础 persona、3 类任务模板，并约束 start_url、success_criteria、max_steps、allowed_actions、risk_level 等字段。
- [ ] 实现受控 Action Schema：定义 click、fill、press、upload、scroll、wait、navigate、ask_for_help、abandon 等允许动作，禁止 LLM 输出任意可执行代码。
- [ ] 实现 Playwright 执行器：启动浏览器会话，执行受控动作，收集当前 URL、标题、DOM 摘要、可交互元素、错误提示、截图和执行结果。
- [ ] 实现页面观察与步骤日志：在每一步行动前后记录 ObservedPageState、decided_action、execution_result、validation_result、screenshot_path 与 friction_signals。
- [x] 搭建 LangGraph 主流程：实现 load_persona、load_task、init_session、observe_page、retrieve_context、decide_action、execute_action、validate_progress、log_step、finalize_report 的基础闭环。
- [ ] 实现验证与停止条件：根据 success_criteria、进展信号、错误次数、重复动作、最大步数判断成功、失败、卡住、偏航或需要恢复。
- [ ] 实现基础 RAG 上下文层：先支持产品知识与历史失败案例导入、向量检索、关键词检索和 metadata 过滤，为决策节点提供简短上下文。
- [ ] 实现失败恢复分支：遇到错误或卡住时检索相似失败案例，选择受控恢复动作，并避免重复尝试无效动作。
- [ ] 实现基础摩擦分析：统计总步数、重复点击、返回、错误恢复、页面停留、迷失和误点信号，生成结构化摩擦问题。
- [ ] 实现报告生成能力：基于 persona、task、执行路径、关键截图、成功失败判定、摩擦摘要和修复建议生成可读报告。
- [ ] 实现后端 API：提供 projects、personas、tasks、runs/start、runs/status、runs/{id}/steps、runs/{id}/report、knowledge/import 等基础接口。
- [ ] 搭建 MVP 前端查看页：支持创建测试任务、选择 persona、启动 run、查看执行状态、步骤日志、截图和最终报告。
- [ ] 加入安全边界：默认只允许测试环境运行，禁止支付、删除、发布等高风险动作，对 destructive action 设置禁止或二次确认。
- [ ] 准备 MVP 验收样例：选择 1 个测试产品、3 个 persona、3 个任务模板，跑通完整链路并保留可复现的示例数据。
