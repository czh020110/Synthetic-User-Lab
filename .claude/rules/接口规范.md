对于 LangChain 和 LangGraph 的接口, 当需要使用以下相关接口时, 优先用这个接口来实现功能, 除非特殊情况:

- LangChain 接口规范
  - 模型接入与调用 ...接口规范md
    - ChatOpenAI
    - ChatOllama
    - OpenAI
    - DashScopeEmbeddings
    - OllamaEmbeddings
    - DashScopeRerank
  - Message 与上下文 ...接口规范.md
    - SystemMessage
    - HumanMessage
    - AIMessage
    - ChatMessage
    - agent.invoke
  - Prompt 模板 ...接口规范.md
    - PromptTemplate.from_template
    - ChatPromptTemplate.from_messages
    - MessagesPlaceholder
  - 输出解析 ...接口规范.md
    - StrOutputParser
    - JsonOutputParser
    - XMLOutputParser
    - CommaSeparatedListOutputParser
  - LCEL 与 Runnable 编排 ...接口规范.md
    - |
    - invoke / stream / batch
    - ainvoke / astream / abatch
    - RunnableParallel
    - RunnablePassthrough.assign
    - itemgetter
    - lambda
    - @chain
  - Memory ...接口规范.md
    - InMemoryChatMessageHistory
    - RunnableWithMessageHistory
    - FileChatMessageHistory
  - RAG 基础链路 ...接口规范.md:192
    - CSVLoader
    - JSONLoader
    - TextLoader
    - PyPDFLoader
    - RecursiveCharacterTextSplitter
    - InMemoryVectorStore
    - Chroma
    - as_retriever()
- LangGraph 接口规范 ...接口规范.md
  - 图与状态建模 ...接口规范.md
    - StateGraph(state_schema, config_schema=None)
    - add_node(name, node_fn)
    - add_edge(src, dst)
    - compile(...)
    - invoke(input, config=...)
    - astream(input, config=...)
    - Annotated[..., add_messages]
  - 条件路由与循环 ...接口规范.md
    - add_conditional_edges(source, path, path_map=None)
  - 子图复用 ...接口规范.md:278
    - 子图作为节点
    - app.invoke(...)
  - 工具调用模式 ...接口规范.md
    - ToolNode(tools=[...])
    - create_agent(model, tools, ...)
  - 结构化输出与类型约束 ...接口规范.md
    - model.with_structured_output(Schema, method=...)
  - 持久化与会话连续 ...接口规范.md
    - InMemorySaver
    - compile(checkpointer=...)
    - config.thread_id
  - HITL 中断与恢复 ...接口规范.md
    - interrupt(...)
    - Command(resume=...)
    - Command(goto=...)
    - compile(interrupt_before=[...], interrupt_after=[...])
  - 时间回溯与分叉 ...接口规范.md
    - get_state(config)
    - get_state_history(config)
    - update_state(old_snapshot.config, values=..., as_node=...)
  - 记忆分层 ...接口规范.md
    - BaseStore
    - InMemoryStore
    - store.put(...)
    - store.search(...)
  - 消息压缩 ...接口规范.md
    - SummarizationMiddleware

> 以上接口的具体实现文档路径为本项目的: `.claude\introduction\笔记接口\LangChain与LangGraph接口规范.md` 中

对于前端设计的页面, 请使用frontend-design skill来设计
